@using SocialBlog.Web.Models.Comment;

@model AllCommentViewModel


@if (!Model.Comments.Any())
{
	<h2 class="text-center">No Post found</h2>
}

<div class="row">
	@foreach (var post in Model.Comments)
	{
		<partial name="_CommentPartial" model="@post" />
	}
</div>
